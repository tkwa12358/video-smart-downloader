# 快速开始指南

本指南帮助你快速构建和运行视频智能下载器。

## 📋 目录

- [开发者快速开始](#开发者快速开始)
- [用户快速开始](#用户快速开始)
- [常见问题](#常见问题)

---

## 👨‍💻 开发者快速开始

### 前置要求

1. **Rust** (>= 1.70)
   - macOS/Linux: `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`
   - Windows: 访问 https://rustup.rs/

2. **Node.js** (>= 18.0)
   - macOS: `brew install node`
   - Windows: 访问 https://nodejs.org/

3. **平台特定工具**
   - macOS: Xcode Command Line Tools (`xcode-select --install`)
   - Windows: 无额外要求

### 5 分钟构建 (macOS)

```bash
# 1. 克隆项目
git clone <your-repo-url>
cd video-smart-downloader

# 2. 运行自动化脚本
chmod +x scripts/*.sh
./scripts/download-binaries.sh  # 下载 yt-dlp 和 ffmpeg
./scripts/build-mac.sh           # 构建应用

# 3. 安装包位置
# src-tauri/target/release/bundle/dmg/video-smart-downloader_1.0.0_universal.dmg
```

### 5 分钟构建 (Windows)

```powershell
# 1. 克隆项目
git clone <your-repo-url>
cd video-smart-downloader

# 2. 运行自动化脚本（PowerShell 管理员模式）
.\scripts\download-binaries.ps1  # 下载 yt-dlp 和 ffmpeg
.\scripts\build-windows.ps1      # 构建应用

# 3. 安装包位置
# src-tauri\target\release\bundle\nsis\video-smart-downloader_1.0.0_x64-setup.exe
```

### 开发模式

```bash
# 安装依赖
npm install

# 启动开发服务器（前端热重载）
npm run tauri:dev
```

---

## 👥 用户快速开始

### 安装

1. 从 [Releases](https://github.com/your-repo/releases) 下载对应平台的安装包
2. 安装并运行应用

### 首次使用

1. **检查网络**
   - 应用启动时会自动检测网络环境
   - 如果需要下载 YouTube 等被墙网站，请启用系统代理

2. **下载视频**
   ```
   粘贴链接 → 点击解析 → 选择清晰度 → 开始下载
   ```

3. **查找下载文件**
   ```
   Windows: C:\Users\你的用户名\Downloads\videocap\视频标题\
   macOS:   /Users/你的用户名/Downloads/videocap/视频标题/
   ```

### 代理设置

**macOS**:
```
系统偏好设置 → 网络 → 高级 → 代理 → 勾选 HTTP/HTTPS 代理
```

**Windows**:
```
设置 → 网络和 Internet → 代理 → 手动设置代理
```

应用会**自动检测**系统代理，无需在应用内配置。

---

## ❓ 常见问题

### Q1: 提示"找不到 yt-dlp"

**Windows**: 确保 `src-tauri\binaries\yt-dlp.exe` 存在
**macOS**: 确保 `src-tauri/binaries/yt-dlp` 存在且有执行权限

```bash
chmod +x src-tauri/binaries/yt-dlp
```

### Q2: macOS 提示"已损坏"

```bash
# 移除隔离属性
xattr -cr /Applications/video-smart-downloader.app
```

### Q3: YouTube 无法下载

1. 检查网络（是否能访问 YouTube）
2. 检查代理设置（应用会自动检测）
3. 点击"重新检测"按钮

### Q4: 下载速度很慢

- 国外平台：需要稳定的代理
- 国内平台：检查网络连接

### Q5: 字幕没有下载

只有**官方认证字幕**会被下载（.srt 格式）。
如果视频没有官方字幕，则不会下载字幕文件。

### Q6: 某个平台不支持

查看 [PLATFORMS.md](./PLATFORMS.md) 了解支持的平台列表。

---

## 📁 项目结构

```
video-smart-downloader/
├── src/                    # React 前端代码
│   ├── App.tsx            # 主应用组件
│   ├── main.tsx           # 入口文件
│   └── index.css          # 全局样式
├── src-tauri/             # Rust 后端代码
│   ├── src/
│   │   └── main.rs        # Rust 主程序
│   ├── binaries/          # yt-dlp 和 ffmpeg
│   ├── Cargo.toml         # Rust 依赖
│   └── tauri.conf.json    # Tauri 配置
├── scripts/               # 构建脚本
│   ├── download-binaries.sh   (macOS)
│   ├── download-binaries.ps1  (Windows)
│   ├── build-mac.sh
│   └── build-windows.ps1
├── package.json           # Node.js 依赖
├── README.md             # 项目说明
├── DEPLOYMENT.md         # 部署文档
├── PLATFORMS.md          # 支持的平台
└── QUICKSTART.md         # 本文件
```

---

## 🛠️ 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Tauri** | 1.5+ | 跨平台桌面框架 |
| **Rust** | 1.70+ | 后端逻辑 |
| **React** | 18.2+ | 前端界面 |
| **TypeScript** | 5.3+ | 类型安全 |
| **Tailwind CSS** | 3.4+ | 样式框架 |
| **yt-dlp** | Latest | 视频下载引擎 |
| **ffmpeg** | Latest | 视频处理 |

---

## 📊 应用特性对比

| 特性 | 本应用 | Grabcube | 在线下载网站 |
|------|--------|----------|------------|
| **体积** | ~85MB | ~200MB | - |
| **内存占用** | ~100MB | ~250MB | - |
| **启动速度** | < 1秒 | ~2秒 | - |
| **代理支持** | ✅ 自动检测 | ⚠️ 手动配置 | ❌ |
| **离线使用** | ✅ | ✅ | ❌ |
| **隐私保护** | ✅ 本地处理 | ✅ | ❌ 上传服务器 |
| **字幕下载** | ✅ 官方字幕 | ✅ 全部字幕 | ⚠️ 部分支持 |
| **更新机制** | ✅ 一键更新 | ✅ | - |

---

## 🚀 下一步

- 阅读 [DEPLOYMENT.md](./DEPLOYMENT.md) 了解详细构建流程
- 查看 [PLATFORMS.md](./PLATFORMS.md) 了解支持的平台
- 提交 Issue 或 PR 参与贡献

---

**祝你使用愉快！🎉**
